// O operador ternário nada mais é do que uma maneira resumida da estrutura de repetição IF, sendo estruturado da seguinte maneira:

// (expressão) ? (se verdadeiro) : (se falso)

const age = 22;
const result = age >= 18 ? "Maior de idade" : "Menor de idade";
console.log(`A pessoa é ${result}`);